#include <iostream>

using namespace std;

int main()
{
    int min, max;
    cin >> min;
    cin >> max;
    cout << (rand() % (max - 1)) + min << + "\n";
    cout << (rand() % (max - 1)) + min << + "\n";
    cout << (rand() % (max - 1)) + min;
    return 0;
}
